# CLAUDE.md

Claude Code가 이 저장소에서 작업할 때 먼저 읽는 문서입니다.

---

## 이 저장소는 무엇인가

코디세이 교육생(마리너)이 세션 플랫폼 **cody-stat(출항일지)** 을 인계받아 운영하기 위한 **문서 저장소**입니다.
서비스 코드는 여기 없습니다. 여기 있는 것은 **의사결정, 기획, 운영 기록, 그리고 공개용 안내 페이지**입니다.

- 인계 개시: 2026년 9월 3일
- 운영 기간: 본과정 약 1년 6개월
- 인원: 약 10명 (SW 전공자 + 비전공자 혼합)
- 재단·퍼실리테이터 미관여 — 예산·기술 지원 없음
- 개발 방식: 바이브코딩 적극 활용. **개발력보다 기획력이 중요한 프로젝트**

---

## 작업 분담

| 도구 | 역할 |
| --- | --- |
| claude.ai (채팅) | 발산, 초안, 구조 설계, 문서 시안 작성 |
| **Claude Code (여기)** | 파일 실제 생성·수정, 일괄 갱신, 링크 검증, 커밋 |
| 이 저장소 | 둘 사이의 단일 출처 |
| 노션 | 팀원이 읽고 댓글 다는 사본 (작업 원본 아님) |

**세션을 시작하면 `HANDOFF.md`를 먼저 읽으세요.** 이전 작업이 어디서 멈췄는지 적혀 있습니다.
**세션을 마칠 때 `HANDOFF.md`를 갱신하세요.** 다음 세션(또는 채팅 쪽 Claude)이 그걸 보고 이어받습니다.

---

## 구조

```
index.html      개요·편성 (공개 페이지)
roles.html      직군 지도 (공개 페이지)
docs.html       문서 체계 (공개 페이지)
style.css       세 페이지 공용 스타일
README.md       프로젝트 전체 안내
HANDOFF.md      세션 간 인계 노트  ← 매번 읽고 매번 갱신
docs/
  DOCUMENT-GUIDE.md   문서 규칙 전문
  00-decisions/       결정 기록 (가장 중요)
  01-handover/        서비스 인계 기록
  02-product/         기획 — backlog, research, specs
  03-tech/            기술 — architecture, data-model, deploy, runbook
  04-ops/             운영 — policy, privacy, release-notes, faq
  05-metrics/         지표
  06-team/            roster, rotation, onboarding
```

문서를 새로 만들 때는 `docs/DOCUMENT-GUIDE.md`의 규칙을 따르세요.

---

## Git 중심 운영

이 저장소가 **단일 출처**입니다. 채팅에서 나온 초안도, 회의에서 정한 것도, 결국 여기 커밋되어야 존재하는 것으로 칩니다. 커밋되지 않은 내용은 없는 내용입니다.

**커밋 단위**

- 문서 하나를 의미 있게 고쳤으면 커밋합니다. 여러 문서를 몰아서 한 번에 커밋하지 않습니다
- 커밋 메시지는 한국어 한 줄. 무엇을 왜 바꿨는지 (`결정로그 추가 — 9월 인계 범위 확정`)
- 작업을 마치면 반드시 `HANDOFF.md`를 갱신하고 함께 커밋합니다

**브랜치**

- **완화 기간 (현재 적용 중, ~팀원 합류 전까지)** — 리뷰할 사람이 없으므로 공개 페이지(`*.html`, `style.css`) 구조 변경을 포함해 **모든 작업을 `main`에 직접 커밋**합니다. 브랜치·PR을 만들지 않습니다
- **복원 조건** — 리뷰어(PO 외 커밋 권한자)가 **2명 이상**이 되는 시점부터 아래 원래 규칙으로 되돌립니다. 복원 시 이 항목과 `00-decisions/decision-log.md`의 관련 줄을 함께 갱신합니다
- **복원 후 원래 규칙**
  - 문서 저장소이므로 기본은 `main`에 직접 커밋합니다. 문서 작업에 PR 리뷰는 과합니다
  - 예외 — 공개 페이지(`*.html`, `style.css`) 구조를 크게 바꿀 때, 편성안·로드맵처럼 팀 합의가 필요한 내용을 통째로 갈아엎을 때는 브랜치를 파고 PR로 올립니다
  - 모바일·웹 세션은 브랜치와 PR로 동작합니다. 작은 수정이라면 PR을 만들고 바로 머지해도 됩니다

**여러 곳에서 작업하기**

이 프로젝트는 데스크톱, 노트북, 모바일에서 번갈아 작업합니다.

- 작업 **시작 전에 `git pull`** 합니다. 다른 기기에서 이미 고쳤을 수 있습니다
- 충돌이 나면 임의로 한쪽을 버리지 말고, 무엇이 충돌했는지 사용자에게 알리고 확인받습니다
- 작업 **끝나면 바로 푸시**합니다. 로컬에만 남은 커밋은 다음 기기에서 보이지 않습니다

**금지**

- `git push --force`, 히스토리 재작성(rebase, amend 후 강제 푸시)은 하지 않습니다
- 사용자가 명시적으로 요청하지 않으면 파일을 삭제하지 않습니다

---

## 지켜야 할 것

**절대 하지 말 것**

- 비밀값(토큰, 비밀번호, API 키)을 어떤 파일에도 적지 않습니다. `.gitignore`에 `.env`, `*.key`, `secrets*`가 있습니다
- 팀원 개인 연락처·이메일을 저장소에 올리지 않습니다
- `00-decisions/decision-log.md`의 기존 줄을 **수정하거나 삭제하지 않습니다**. 결정이 번복되면 새 줄을 추가합니다

**항상 할 것**

- 문서 맨 위에 `담당: OOO · 최종 갱신: YYYY-MM-DD`를 적습니다
- 확정되지 않은 내용은 `(초안)` 또는 `TBD`로 표시합니다
- 세 HTML 페이지 중 하나를 고치면 나머지 두 개의 네비게이션·링크도 확인합니다
- 커밋 메시지는 한국어로, 무엇을 왜 바꿨는지 한 줄로 씁니다

---

## 문서 톤

- 존댓말, 담백하게. 감탄사나 이모지는 쓰지 않습니다
- 짧은 문장. 한 문단은 3~4줄을 넘기지 않습니다
- 결론을 먼저, 배경은 뒤에
- 독자는 **PM·기획 경험이 없는 교육생**입니다. 업계 용어를 쓸 때는 한 번은 풀어 씁니다
- 잘라내도 뜻이 통하면 잘라냅니다

---

## 자주 하는 작업

**결정 기록하기**
`docs/00-decisions/decision-log.md`에 `| 날짜 | 결정 | 배경 | 결정자 |` 한 줄 추가.
되돌리기 어려운 것, 돈이 드는 것, 여러 사람에게 영향 가는 것이 대상입니다.

**큰 결정은 ADR로**
`docs/00-decisions/adr/YYYY-MM-DD-제목.md` — 배경 / 선택지 / 결정 / 이유 / **버린 것**.
버린 선택지를 적는 것이 이 문서의 핵심입니다.

**공개 페이지 수정**
`style.css`의 CSS 변수만 씁니다. 새 색을 만들지 않습니다.
팔레트: `--deep --hull --hull-2 --line --parch --steel --teal --brass --coral`

**인터뷰 기록**
`docs/02-product/research/YYYY-MM-DD-누구.md`.
들은 말을 요약하지 말고 그대로 적습니다. 해석은 바뀌지만 발언은 안 바뀝니다.
